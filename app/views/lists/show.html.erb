<div class="container">
   <h1><%= @list.name %></h1>
     <% @list.bookmarks.each do |bookmark|%>

      <%= bookmark.comment %>
      <%= bookmark.movie.title %> <br>
      <%= image_tag bookmark.movie.poster_url %>
      <%= link_to "🗑️", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    <%   end %>
    <br>
  <br>
  <%=link_to "make a new bookmark", new_list_bookmark_path(@list)%>
  <br>
  <%=link_to "back to the list", lists_path%>

</div>
